{
  "scripts": {
    "lint": "gdlint **/*.gd && prettier --check '**/*.{json,md}' && sort-package-json --check",
    "lint:fix": "gdformat **/*.gd && prettier --write '**/*.{json,md}' && sort-package-json",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^15.0.2",
    "prettier": "^3.0.3",
    "prettier-plugin-sort-json": "^3.1.0",
    "sort-package-json": "^2.6.0"
  }
}
